You asked to **add Circular Queue** while keeping the same format (only question + input/output).
Here’s the updated **Markdown file with 4 questions**:

---

```markdown id="qmd456"
# Queue Questions for Placement

---

## 1. Queue Implementation Using Array

### Problem
Implement a queue using array with the following operations:
- enqueue(x)
- dequeue()
- front()
- isEmpty()
- size()

### Input
```

enqueue(10), enqueue(20), enqueue(30), front(), dequeue(), front()

```id="n9fx5s"

### Output
```

front() → 10
dequeue() → 10
front() → 20

```id="3g4znt"

---

## 2. First Non-Repeating Character in a Stream

### Problem
Given a stream of characters, for each character, return the first non-repeating character.

### Input
```

a a b c

```id="p7g7qk"

### Output
```

a -1 b b

```id="zq7b6d"

---

## 3. Sliding Window Maximum

### Problem
Given an array and a window size `k`, find the maximum element in each sliding window.

### Input
```

arr = [1,3,-1,-3,5,3,6,7], k = 3

```id="q0vhcu"

### Output
```

[3,3,5,5,6,7]

```id="x3t8lb"

---

## 4. Circular Queue (Design)

### Problem
Design a circular queue with fixed size supporting:
- enqueue(x)
- dequeue()
- front()
- rear()
- isFull()
- isEmpty()

### Input
```

size = 3
enqueue(1), enqueue(2), enqueue(3), dequeue(), enqueue(4)

```id="b7c7m2"

### Output
```

front → 2
rear → 4

```id="6mjlwm"

---
```
